if messages.errors
  .alert.alert-red.fade.in.alert-dismissable
    button.close.closing-btn(type='button', data-dismiss='alert') ✕
    for error in messages.errors
      div.msg= error.msg
if messages.info
  .alert.alert-blue.alert-dismissable
    button.close.closing-btn(type='button', data-dismiss='alert') ✕
    for info in messages.info
      div.msg= info.msg
if messages.success
  .alert.alert-green.alert-dismissable
    button.close.closing-btn(type='button', data-dismiss='alert') ✕
    for success in messages.success
      div.msg= success.msg
